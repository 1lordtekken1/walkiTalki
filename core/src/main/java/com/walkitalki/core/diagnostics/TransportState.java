package com.walkitalki.core.diagnostics;

public enum TransportState {
    IDLE,
    SCANNING,
    PAIRING_REQUIRED,
    CONNECTING,
    CONNECTED,
    TRANSMITTING,
    RECEIVING,
    DISCONNECTED
}
